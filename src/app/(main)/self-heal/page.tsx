import { SelfHealView } from "@/components/SelfHealView";

export const dynamic = "force-dynamic";

// 自我进化 · 自愈中心:失败自动入账、AI 分析、有界自动修复
export default function SelfHealPage() {
  return <SelfHealView />;
}
